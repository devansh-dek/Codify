export type Problem = {
    title: string,
    description: string,
    sampleInput: string,
    sampleOutput: string,
    difficulty: string,
    constraint: string,
    explanation: string,
    inputFormat: string,
    outputFormat: string
}